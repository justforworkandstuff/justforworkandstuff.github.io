export enum RouterPath {
  Initial = "/"
}

export enum MenuType {
  About = "About",
  Projects = "Projects",
  Contact = "Contact"
}

export enum SkillsType {
  Mobile = "Mobile",
  FrontEnd = "FrontEnd",
  BackEnd = "BackEnd",
  Tools = "Tools"
}